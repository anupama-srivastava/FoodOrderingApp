<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".FoodOrderingApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodOrderingApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".presentation.ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.FoodOrderingApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.ui.auth.AuthActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".presentation.ui.main.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".presentation.ui.restaurant.RestaurantDetailActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.main.MainActivity" />

        <activity
            android:name=".presentation.ui.cart.CartActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.main.MainActivity" />

        <activity
            android:name=".presentation.ui.checkout.CheckoutActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.cart.CartActivity" />

        <activity
            android:name=".presentation.ui.order.OrderTrackingActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.main.MainActivity" />

        <activity
            android:name=".presentation.ui.profile.ProfileActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.main.MainActivity" />

        <activity
            android:name=".presentation.ui.search.SearchActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.main.MainActivity" />

        <activity
            android:name="com.razorpay.CheckoutActivity"
            android:configChanges="keyboard|keyboardHidden|screenSize|orientation|screenLayout|smallestScreenSize"
            android:exported="true"
            android:theme="@style/Theme.FoodOrderingApp">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="payment"
                    android:scheme="foodorderingapp" />
            </intent-filter>
        </activity>

        <service
            android:name=".data.remote.firebase.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

    </application>

</manifest>
